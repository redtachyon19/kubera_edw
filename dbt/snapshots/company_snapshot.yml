version: 2

snapshots:
  - name: company_snapshot
    relation: ref('seed_companies')
    config:
      schema: snapshots
      unique_key: ticker
      strategy: check
      check_cols:
        - sector
        - industry
        - filer_type
        - country_iso3
        - currency_iso
        - reporting_currency
        - xbrl_taxonomy
