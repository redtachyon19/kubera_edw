# --- HTTP / extraction ---
httpx>=0.27
requests>=2.32
tenacity>=8.2          # retry/backoff for flaky free-tier APIs
python-dotenv>=1.0     # load .env locally

# --- Data handling ---
pandas>=2.2
pyarrow>=16.0          # parquet landing for raw responses
PyYAML>=6.0            # read ingestion/config/companies.yml

# --- Warehouse ---
duckdb>=1.0            # local dev warehouse
psycopg2-binary>=2.9   # Postgres (containerized)

# --- Transformation / modeling ---
# Upper bound on dbt-core is dictated by dagster-dbt, which pins a compatible range;
# installing dagster-dbt against an unpinned dbt-core silently downgrades it.
dbt-core>=1.11,<1.12
dbt-duckdb>=1.8        # dev adapter
dbt-postgres>=1.8      # containerized adapter

# --- Orchestration ---
dagster>=1.13
dagster-webserver>=1.13
dagster-dbt>=0.29        # @dbt_assets: models become first-class Dagster assets

# --- BI ---
streamlit>=1.36
altair>=5.0            # charting (ships with streamlit)
vl-convert-python>=1.0 # render charts to PNG for the README

# --- Testing / quality ---
pytest>=8.2
pytest-mock>=3.14
respx>=0.21            # mock httpx calls in tests
ruff>=0.5             # lint + format
